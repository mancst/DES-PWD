// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("DES-PWE", "for DES-PWE support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("DES-PWE", "enable DES-PWE support", "no");

if (PHP_DES-PWE != "no") {
	EXTENSION("DES-PWE", "DES-PWE.c");
}

